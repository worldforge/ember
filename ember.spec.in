%define name @PACKAGE@
%define version @VERSION@
%define release 1

Summary: Ember - a client for Worldforge
Name: %{name}
Version: %{version}
Release: %{release}
Source0: %{name}-%{version}.tar.gz
URL: http://www.worldforge.org/dev/eng/clients/ember
Copyright: GPL
Group: Amusements/Games
BuildRoot: /var/tmp/%{name}-buildroot
Prefix: %{_prefix}
Requires: eris >= 1.3 mercator varconf ogre >= 0.15.0 boost

%description
Ember is a client for MMORPGs using the Worldforge system.
It uses the Ogre 3d engine with CEGUI.
Right now Ember is not fully functional, but it's getting there. 


%prep
rm -rf ${RPM_BUILD_ROOT}

%setup -q 

%build
CFLAGS="$RPM_OPT_FLAGS" 
./configure --prefix=%{prefix} --enable-final=full
make -j 4

%install
rm -rf $RPM_BUILD_ROOT
make install prefix=$RPM_BUILD_ROOT/%{prefix}

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root)
%doc COPYING README INSTALL AUTHORS doc/user_guide.txt
#%{prefix}/lib/*
%{prefix}/share/*
%{prefix}/bin/*


# end of file
