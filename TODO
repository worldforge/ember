TODO LIST
---------

terrain
	-implement paging
	-better use of GPU shaders (both vertex and pixel shaders)
	-optimize material better
	
foliage
	-research the best way to present foliage, perhaps it would be better to not use static geometry but use a fixed set of entites instead?

sun
	-implement time, making the sun move over the sky etc.
	
* a map widget, which shows a map of the world
* add a constant-running key
* move the input system to something more like the one used in sear, which will allow the user to define the key bindings for different actions
* add a basepoint editor
* add mouselook toggle, perhaps caps lock
* add a check for nvidia cards and SGI Mesa libGL, and if so warn the user




